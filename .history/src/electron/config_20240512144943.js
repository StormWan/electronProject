// 主窗口配置
global.mainWinOptions = {
  width: 1440,
  height: 800,
  minWidth: 1440,
  minHeight: 800,
};

// 登录窗口配置
global.loginWinOptions = {
  width: 380,
  height: 550,
  maxWidth: 380,
  maxHeight: 550,
  minWidth: 380,
  minHeight: 550,
  frame: true, // 创建无边框窗口
  resizable: false, //禁止改变窗口尺寸
};
